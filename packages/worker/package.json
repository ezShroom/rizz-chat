{
	"name": "worker",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"build": "wrangler build",
		"types": "wrangler types",
		"check": "tsc",
		"lint": "eslint .",
		"style": "prettier -c .",
		"fix_style": "prettier -w .",
		"deploy": "wrangler deploy"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"shared": "workspace:*",
		"drizzle-orm": "^0.44.2"
	}
}
